#include <stdio.h>
#include <math.h>

int main(void) {
    /**
     * Tabla de resultados de la primera sucesión.
     */

    int k = 10;
    float s4 = sqrtf(2);

    int sk_1, sk_2;

    for (int i = 2; i < 2*k ; i+=2) {

        printf("||\tNúmero  de vértices: %d\t||\tValor de la aproximación: %le\t||\tError relativo: %le", k, )
    }


    

    return 0;
}